// 4. Convert this function to an arrow function: function square(n) { return n * n) }

const square = (n) => n * n;
console.log(square(2));


//Merge the arrays ['Raj', 'Amit'] and ['Sita', 'Geeta'] into one.

let arr1 = ['Raj', 'Amit'];
let arr2 = ['Sita', 'Geeta'];
console.log(arr1.concat(arr2));